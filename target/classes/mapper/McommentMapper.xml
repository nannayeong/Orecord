<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="impl.McommentImpl">
	
	<!-- 회원리스트보기 -->
	<select id="audioCommentCount" resultType="int">
		SELECT count(*) FROM m_comment where audio_idx=#{param1}
	</select>

</mapper>